# Leave NEXT_PUBLIC_API_URL empty for web (uses relative URLs)
NEXT_PUBLIC_API_URL=

# Upstash Redis (for leaderboard + rate limiting in production)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
